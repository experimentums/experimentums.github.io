# Experimentums website sources

## Development

Setup development server using:

```
npm install http-server -g
```

Go to the `src` directory and run development server:

```
http-server -c-1 --log-ip
```
